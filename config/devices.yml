- name: dockbox
  devices:
    - name: router
      ip: 192.168.0.1
      diagnose:
        name: Validate {{plateform.name}} router availability
        successMessage: "Diagnosed {{plateform.name}} {{device.name}} successfully"
        errorMessage: "An error occur while diagnosing {{plateform.name}}"
        actions:
          - name: "Ping {{plateform.name}} {{device.name}}"
            successMessage: "{{plateform.name}} {{device.name}} is available"
            errorMessage: "{{plateform.name}} {{device.name}} seems to be unavailable"
            cmd: "ping {{device.ip}} -n 5"
- name: auv7
  devices:
    - name: switch
      ip: 192.168.0.10
      diagnose:
        name: Validate {{plateform.name}} {{device.name}} availability
        successMessage: "Diagnosed {{plateform.name}} {{device.name}} successfully"
        errorMessage: "An error occur while diagnosing {{plateform.name}} {{device.name}}"
        actions:
          - name: "Ping {{plateform.name}} {{device.name}}"
            successMessage: "{{plateform.name}} {{device.name}} is available"
            errorMessage: "{{plateform.name}} {{device.name}} seems to be unavailable"
            cmd: "ping {{device.ip}} -n 5"
    - name: computer
      ip: 192.168.0.11
      diagnose:
        name: Validate {{plateform.name}} {{device.name}} availability
        successMessage: "Diagnosed {{plateform.name}} {{device.name}} successfully"
        errorMessage: "An error occur while diagnosing {{plateform.name}} {{device.name}}"
        actions:
          - name: "Ping {{plateform.name}} {{device.name}}"
            successMessage: "{{plateform.name}} {{device.name}} is available"
            errorMessage: "{{plateform.name}} {{device.name}} seems to be unavailable"
            cmd: "ping {{device.ip}} -n 5"
    - name: sonar
      ip: 192.168.0.12
      diagnose:
        name: Validate {{plateform.name}} {{device.name}} availability
        successMessage: "Diagnosed {{plateform.name}} {{device.name}} successfully"
        errorMessage: "An error occur while diagnosing {{plateform.name}} {{device.name}}"
        actions:
          - name: "Ping {{plateform.name}} {{device.name}}"
            successMessage: "{{plateform.name}} {{device.name}} is available"
            errorMessage: "{{plateform.name}} {{device.name}} seems to be unavailable"
            cmd: "ping {{device.ip}} -n 5"
- name: auv8
  devices:
    - name: switch
      ip: 192.168.0.30
      diagnose:
        name: Validate {{plateform.name}} {{device.name}} availability
        successMessage: "Diagnosed {{plateform.name}} {{device.name}} successfully"
        errorMessage: "An error occur while diagnosing {{plateform.name}} {{device.name}}"
        actions:
          - name: "Ping {{plateform.name}} {{device.name}}"
            successMessage: "{{plateform.name}} {{device.name}} is available"
            errorMessage: "{{plateform.name}} {{device.name}} seems to be unavailable"
            cmd: "ping {{device.ip}} -n 5"
    - name: computer
      ip: 192.168.0.31
      diagnose:
        name: Validate {{plateform.name}} {{device.name}} availability
        successMessage: "Diagnosed {{plateform.name}} {{device.name}} successfully"
        errorMessage: "An error occur while diagnosing {{plateform.name}} {{device.name}}"
        actions:
          - name: "Ping {{plateform.name}} {{device.name}}"
            successMessage: "{{plateform.name}} {{device.name}} is available"
            errorMessage: "{{plateform.name}} {{device.name}} seems to be unavailable"
            cmd: "ping {{device.ip}} -n 5"
    - name: sonar
      ip: 192.168.0.32
      diagnose:
        name: Validate {{plateform.name}} {{device.name}} availability
        successMessage: "Diagnosed {{plateform.name}} {{device.name}} successfully"
        errorMessage: "An error occur while diagnosing {{plateform.name}} {{device.name}}"
        actions:
          - name: "Ping {{plateform.name}} {{device.name}}"
            successMessage: "{{plateform.name}} {{device.name}} is available"
            errorMessage: "{{plateform.name}} {{device.name}} seems to be unavailable"
            cmd: "ping {{device.ip}} -n 5"
